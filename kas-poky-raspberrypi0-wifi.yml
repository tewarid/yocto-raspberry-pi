header:
  version: 8

machine: raspberrypi0-wifi
distro: poky
target:
  - core-image-base

repos:
  meta-raspberry:
    url: http://git.yoctoproject.org/git/meta-raspberrypi
    path: layers/meta-raspberrypi
    refspec: f9a13f3b9a90f4f033afa9f34b66ca7432a7742b

  poky:
    url: http://git.yoctoproject.org/git/poky
    path: layers/poky
    refspec: ea455ca8671d3bc2a1097989bfaabe92f3ca37ab
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:

  meta-openembedded:
    url: git://git.openembedded.org/meta-openembedded
    path: layers/meta-openembedded
    refspec: ba2576ed79a09879f714ccf7f4fa32259c18ab8c
    layers:
      meta-oe:
      meta-python:
      meta-networking:

bblayers_conf_header:
  standard: |
    POKY_BBLAYERS_CONF_VERSION = "2"
    BBPATH = "${TOPDIR}"
    BBFILES ?= ""
local_conf_header:
  standard: |
    CONF_VERSION = "1"
    PACKAGE_CLASSES = "package_rpm"
    SDKMACHINE = "x86_64"
    USER_CLASSES = "buildstats image-mklibs image-prelink"
    PATCHRESOLVE = "noop"
  debug-tweaks: |
    EXTRA_IMAGE_FEATURES = "debug-tweaks"
  diskmon: |
    BB_DISKMON_DIRS = "\
        STOPTASKS,${TMPDIR},1G,100K \
        STOPTASKS,${DL_DIR},1G,100K \
        STOPTASKS,${SSTATE_DIR},1G,100K \
        STOPTASKS,/tmp,100M,100K \
        ABORT,${TMPDIR},100M,1K \
        ABORT,${DL_DIR},100M,1K \
        ABORT,${SSTATE_DIR},100M,1K \
        ABORT,/tmp,10M,1K"
  systemd: |
    DISTRO_FEATURES_append = " systemd"
    VIRTUAL-RUNTIME_init_manager = "systemd"
  sshd: |
    EXTRA_IMAGE_FEATURES_append = " ssh-server-dropbear"
  wifi: |
    IMAGE_INSTALL_append = " hostapd dnsmasq"
  python: |
    IMAGE_INSTALL_append = " python3 python3-dbus"
  audio: |
    IMAGE_INSTALL_append = " alsa-utils"
